/* PaddleOCR macOS 桌面应用 — 全局 QSS 主题 v3 */

QMainWindow {
    background-color: #F5F5F7;
}

/* ── 侧边栏 ── */
#sidebar {
    background-color: #FFFFFF;
    border-right: 1px solid #E5E5EA;
}

#sidebar QPushButton#sidebarNavBtn {
    text-align: left;
    padding: 8px 12px;
    border: none;
    border-radius: 8px;
    border-left: 3px solid transparent;
    font-size: 13px;
    font-weight: 500;
    color: #3C3C43;
    background: transparent;
    margin: 1px 0;
}

#sidebar QPushButton#sidebarNavBtn:hover {
    background-color: #F2F2F7;
    color: #1D1D1F;
}

#sidebar QPushButton#sidebarNavBtn:checked {
    background-color: #E8F0FE;
    border-left: 3px solid transparent;
    color: #1A73E8;
    font-weight: 600;
}

/* ── 格式卡片（使用 objectName 选择器） ── */
QWidget#formatCard {
    background-color: #FFFFFF;
    border: 1.5px solid #E5E5EA;
    border-radius: 10px;
}

QWidget#formatCard:hover {
    border-color: #1A73E8;
    background-color: #FAFBFF;
}

/* ── 主按钮 ── */
#startButton {
    background: qlineargradient(
        x1:0, y1:0, x2:1, y2:0,
        stop:0 #1A73E8, stop:1 #4A9CF5
    );
    color: white;
    border: none;
    border-radius: 10px;
    padding: 12px 36px;
    font-size: 15px;
    font-weight: 600;
}

#startButton:hover {
    background: qlineargradient(
        x1:0, y1:0, x2:1, y2:0,
        stop:0 #1557B0, stop:1 #3A8CE5
    );
}

#startButton:pressed {
    background: qlineargradient(
        x1:0, y1:0, x2:1, y2:0,
        stop:0 #0D47A1, stop:1 #1A73E8
    );
    padding: 13px 35px;
}

#startButton:disabled {
    background: #D1D1D6;
}

/* ── 进度条 ── */
QProgressBar {
    border: none;
    border-radius: 5px;
    background-color: #E5E5EA;
    text-align: center;
    height: 10px;
}

QProgressBar::chunk {
    background: qlineargradient(
        x1:0, y1:0, x2:1, y2:0,
        stop:0 #1A73E8, stop:1 #4A9CF5
    );
    border-radius: 5px;
}

/* ── 分组框 ── */
QGroupBox {
    font-size: 13px;
    font-weight: 600;
    color: #3C3C43;
    border: 1px solid #E5E5EA;
    border-radius: 10px;
    margin-top: 12px;
    padding-top: 20px;
    background-color: #FFFFFF;
}

QGroupBox::title {
    subcontrol-origin: margin;
    left: 16px;
    padding: 0 6px;
    color: #3C3C43;
}

QGroupBox:disabled {
    color: #AEAEB2;
    border-color: #F0F0F2;
    background-color: #FAFAFA;
}

/* ── 下拉框 ── */
QComboBox {
    border: 1px solid #E5E5EA;
    border-radius: 8px;
    padding: 6px 12px;
    background-color: #FFFFFF;
    font-size: 13px;
    color: #1D1D1F;
}

QComboBox:hover {
    border-color: #1A73E8;
}

QComboBox:focus {
    border-color: #1A73E8;
    border-width: 2px;
}

QComboBox::drop-down {
    border: none;
    width: 24px;
}

/* ── 数字输入框 ── */
QSpinBox, QDoubleSpinBox {
    border: 1px solid #E5E5EA;
    border-radius: 6px;
    padding: 4px 8px;
    background-color: #FFFFFF;
    font-size: 13px;
}

QSpinBox:hover, QDoubleSpinBox:hover {
    border-color: #1A73E8;
}

QSpinBox:focus, QDoubleSpinBox:focus {
    border-color: #1A73E8;
    border-width: 2px;
}

/* ── 复选框 ── */
QCheckBox {
    font-size: 13px;
    color: #3C3C43;
    spacing: 8px;
}

QCheckBox:disabled {
    color: #AEAEB2;
}

QCheckBox::indicator {
    width: 18px;
    height: 18px;
    border-radius: 4px;
    border: 2px solid #C7C7CC;
    background-color: #FFFFFF;
}

QCheckBox::indicator:checked {
    background-color: #1A73E8;
    border-color: #1A73E8;
}

QCheckBox::indicator:hover {
    border-color: #1A73E8;
}

QCheckBox::indicator:disabled {
    border-color: #E5E5EA;
    background-color: #F5F5F7;
}

/* ── 滚动区域 ── */
QScrollArea {
    border: none;
    background: transparent;
}

QScrollBar:vertical {
    background: transparent;
    width: 6px;
    margin: 2px 1px;
}

QScrollBar::handle:vertical {
    background: rgba(174, 174, 178, 0.5);
    border-radius: 3px;
    min-height: 30px;
}

QScrollBar::handle:vertical:hover {
    background: rgba(174, 174, 178, 0.8);
}

QScrollBar::add-line:vertical, QScrollBar::sub-line:vertical {
    height: 0;
}

QScrollBar:horizontal {
    background: transparent;
    height: 6px;
    margin: 1px 2px;
}

QScrollBar::handle:horizontal {
    background: rgba(174, 174, 178, 0.5);
    border-radius: 3px;
    min-width: 30px;
}

QScrollBar::handle:horizontal:hover {
    background: rgba(174, 174, 178, 0.8);
}

QScrollBar::add-line:horizontal, QScrollBar::sub-line:horizontal {
    width: 0;
}

/* ── 通用按钮（次要按钮） ── */
QPushButton {
    background-color: #F5F5F7;
    border: 1px solid #E5E5EA;
    border-radius: 8px;
    padding: 6px 16px;
    font-size: 13px;
    color: #3C3C43;
}

QPushButton:hover {
    background-color: #E8F0FE;
    border-color: #1A73E8;
    color: #1A73E8;
}

QPushButton:pressed {
    background-color: #D1D1D6;
    padding: 7px 15px;
}

QPushButton:focus {
    border-color: #1A73E8;
    border-width: 2px;
}

QPushButton:disabled {
    background-color: #F5F5F7;
    color: #AEAEB2;
    border-color: #F0F0F2;
}

/* ── 文本输入框 ── */
QLineEdit {
    border: 1px solid #E5E5EA;
    border-radius: 8px;
    padding: 6px 12px;
    background-color: #FFFFFF;
    font-size: 13px;
    color: #1D1D1F;
}

QLineEdit:focus {
    border-color: #1A73E8;
    border-width: 2px;
}

/* ── 工具提示 ── */
QToolTip {
    background-color: #1D1D1F;
    color: #FFFFFF;
    border: none;
    border-radius: 6px;
    padding: 6px 10px;
    font-size: 12px;
}

/* ── 分割器手柄 ── */
QSplitter::handle {
    background-color: #E5E5EA;
    width: 1px;
    margin: 40px 4px;
}

QSplitter::handle:hover {
    background-color: #1A73E8;
    width: 3px;
    margin: 20px 3px;
}
